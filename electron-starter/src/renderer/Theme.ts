import { createTheme } from '@mui/material'

/**
 * Theme for ThemeProvider (mui).
 */
export const Theme = createTheme({
  palette: {
    primary: {
      main: '#3498db',
      light: '#9fd6fe',
      dark: '#2980b9'
    }
  }
})
